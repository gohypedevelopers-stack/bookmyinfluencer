# Database Connection
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
# Optional for Neon migrations if you want a non-pooled direct connection.
# If you don't have a separate direct connection string, you can leave this unset.
DIRECT_URL=""

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="super-secret-key-change-this"

# OTP + Session (Email verification)
# AUTH_SECRET is used to sign the httpOnly `session` cookie for OTP onboarding.
# You can reuse NEXTAUTH_SECRET, but using a dedicated secret is recommended.
AUTH_SECRET="super-secret-auth-key-change-this"
OTP_SECRET="super-secret-otp-key-change-this"

# SMTP (for sending OTP emails)
# If you want to use Gmail, create an App Password and set SMTP_PASS to it.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-user@example.com"
SMTP_PASS="your-16-character-google-app-password"
# Optional override. For Gmail, this must be the same Gmail account or a verified Gmail alias.
FROM_EMAIL="BookMyInfluencer <your-smtp-user@example.com>"
# Optional: If you have Resend setup, provide the API key and SMTP vars can be empty.
RESEND_API_KEY="re_123456789"

# Cloudinary (Optional for File Uploads)
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# Apify (Social Media Scraping)
APIFY_TOKEN="your_apify_api_token"
APIFY_INSTAGRAM_ACTOR_ID="username/actor-name" # e.g. apify/instagram-scraper
APIFY_YOUTUBE_ACTOR_ID="username/actor-name"   # e.g. streampot/youtube-scraper

# Cloudflare R2 (Secure Storage for KYC)
R2_ACCOUNT_ID="your-account-id"
R2_ACCESS_KEY_ID="your-access-key-id"
R2_SECRET_ACCESS_KEY="your-secret-access-key"
R2_BUCKET_NAME="bookmyinfluencers-kyc"
R2_PUBLIC_BASE_URL="your-r2-public-url" # Optional if using signed URLs
R2_REGION="auto"
